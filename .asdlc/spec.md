# Overview

A basic web application that displays "Hello World" when someone visits it.

# Personas

- Visitor — views the greeting message in their browser.

# Features

- A visitor opens the web application in their browser.
- The page displays "Hello World" as text.
- The application runs on a web server that handles incoming requests.
- The greeting appears immediately when the page loads.
- The page works in standard web browsers.